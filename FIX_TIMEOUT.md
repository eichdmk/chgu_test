# 🚨 СРОЧНОЕ ИСПРАВЛЕНИЕ: ERR_TIMED_OUT для статических файлов (Vercel)

## Приоритет 1: Критические исправления (сделать СЕЙЧАС)

### ✅ 1. Передеплойте проект на Vercel

```bash
vercel --prod
```

**Важно:** `vercel.json` уже настроен правильно ✅

### ✅ 2. Проверьте настройки домена в Vercel Dashboard

1. Откройте проект → Settings → Domains
2. Убедитесь, что домен `www.chgumk.ru` добавлен
3. Проверьте статус: должно быть "Valid Configuration"
4. SSL сертификат должен быть активен

### ✅ 2. Проверьте размер файлов

```bash
ls -lh dist/assets/
```

Если файлы > 1MB, проблема может быть в таймаутах. Решение:
- Увеличьте `proxy_read_timeout` до 300s
- Или оптимизируйте бандлы (уже настроено в vite.config.js)

### ✅ 3. Проверьте DNS и CDN (Cloudflare) - КРИТИЧНО!

**Если используете Cloudflare:**

1. **DNS записи:**
   - Тип: `CNAME`
   - Имя: `www` (или `@` для корневого домена)
   - Цель: `cname.vercel-dns.com`
   - Проксирование: **ВКЛЮЧЕНО** (оранжевое облако) ✅

2. **SSL/TLS настройки:**
   - Режим: **Full** или **Full (strict)** ✅
   - **НЕ используйте "Flexible"** - это вызывает ERR_TIMED_OUT!

3. **Оптимизация:**
   - Speed → Optimization → Auto Minify: **ВЫКЛЮЧИТЕ** для JS/CSS
   - Может ломать файлы и вызывать таймауты

4. **Кеширование:**
   - Caching → Configuration → Browser Cache TTL: "Respect Existing Headers"
   - Vercel уже настроил правильные заголовки в `vercel.json`

## Приоритет 2: Диагностика

### Проверьте доступность файлов:

```bash
# Замените на реальные имена файлов из dist/assets/
curl -I https://www.chgumk.ru/assets/index-B9CQTVkP.js
curl -I https://www.chgumk.ru/assets/index-FbaISL3w.css
```

**Ожидаемый ответ:**
```
HTTP/2 200
Content-Type: application/javascript
Cache-Control: public, max-age=31536000, immutable
```

**Если получаете таймаут:**
- Проблема в конфигурации сервера/прокси
- Проверьте логи: `tail -f /var/log/nginx/error.log`

## Приоритет 3: Оптимизация

1. ✅ `vite.config.js` обновлен с правильными настройками
2. ✅ `vercel.json` создан и настроен для Vercel
3. ✅ Rewrites для React Router настроены
4. ✅ Кеширование статики настроено (1 год для `/assets/`)

## Быстрая проверка для Vercel

1. **Пересоберите проект локально (для проверки):**
```bash
npm run build
```

2. **Проверьте `dist/index.html`** - пути должны быть `/assets/...` (абсолютные)

3. **Деплой на Vercel:**
```bash
vercel --prod
```

4. **Проверьте доступность файлов:**
```bash
# Замените на реальные имена из последнего деплоя
curl -I https://www.chgumk.ru/assets/index-xxx.js
```

Должен вернуть:
```
HTTP/2 200
Cache-Control: public, max-age=31536000, immutable
```

5. **Проверьте в браузере:**
   - Откройте DevTools → Network
   - Обновите страницу
   - Проверьте статус загрузки `/assets/*.js` и `/assets/*.css`

## Частые причины ERR_TIMED_OUT:

1. ❌ **Таймауты слишком короткие** → Увеличьте до 300s
2. ❌ **Неправильная конфигурация прокси** → Используйте `nginx.conf.example`
3. ❌ **Проблемы с CDN** → Проверьте настройки Cloudflare
4. ❌ **Файлы слишком большие** → Уже оптимизировано в vite.config.js
5. ❌ **Проблемы с SSL** → Проверьте сертификат

## Следующие шаги:

1. Примените конфигурацию из `nginx.conf.example` (если свой сервер)
2. Или убедитесь, что `vercel.json` применен (если Vercel)
3. Пересоберите и передеплойте
4. Проверьте доступность файлов через curl
5. Проверьте в браузере с DevTools → Network

