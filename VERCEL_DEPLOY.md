# 🚀 Деплой на Vercel

## Быстрый старт

1. **Установите Vercel CLI** (если еще не установлен):
```bash
npm i -g vercel
```

2. **Войдите в Vercel**:
```bash
vercel login
```

3. **Деплой**:
```bash
# Первый деплой (выберите настройки)
vercel

# Продакшн деплой
vercel --prod
```

## Настройка кастомного домена

### В Vercel Dashboard:

1. Откройте проект → Settings → Domains
2. Добавьте домен: `www.chgumk.ru` и `chgumk.ru`
3. Следуйте инструкциям по настройке DNS:
   - Если используете Cloudflare: CNAME → `cname.vercel-dns.com`
   - Или A-записи, как указано в Vercel

### Важно для кастомного домена:

- ✅ SSL сертификат выдается автоматически
- ✅ `vercel.json` уже настроен правильно
- ✅ Rewrites для React Router настроены
- ✅ Кеширование статики настроено

## Решение проблемы ERR_TIMED_OUT

Если на `*.vercel.app` всё работает, а на кастомном домене статика не загружается:

### 1. Проверьте DNS настройки

**Если используете Cloudflare:**
- DNS записи должны указывать на Vercel
- Проксирование (оранжевое облако) должно быть ВКЛЮЧЕНО
- SSL/TLS режим: **Full** или **Full (strict)**
- НЕ используйте "Flexible" режим SSL

### 2. Проверьте настройки домена в Vercel

- Settings → Domains → ваш домен
- Убедитесь, что статус: "Valid Configuration"
- SSL сертификат должен быть активен

### 3. Очистите кеш

После изменений в DNS:
```bash
# Передеплойте проект
vercel --prod

# Или через Dashboard: Deployments → Redeploy
```

### 4. Проверьте доступность файлов

```bash
# Замените на реальные имена из dist/assets/
curl -I https://www.chgumk.ru/assets/index-xxx.js
```

Должен вернуть:
```
HTTP/2 200
Cache-Control: public, max-age=31536000, immutable
```

## Структура проекта для Vercel

Vercel автоматически определяет Vite проекты, но `vercel.json` уже настроен:

- ✅ `buildCommand`: `npm run build`
- ✅ `outputDirectory`: `dist`
- ✅ Rewrites для SPA routing
- ✅ Headers для кеширования

## Автоматический деплой через Git

1. Подключите репозиторий в Vercel Dashboard
2. Vercel автоматически будет деплоить при push в main/master
3. Preview деплои создаются для каждого PR

## Проверка после деплоя

1. Откройте `https://www.chgumk.ru`
2. Откройте DevTools → Network
3. Проверьте загрузку файлов:
   - `/assets/index-xxx.js` → должен быть 200
   - `/assets/index-xxx.css` → должен быть 200
   - `/assets/react-vendor-xxx.js` → должен быть 200

Если видите ERR_TIMED_OUT:
- Проверьте DNS (должно быть CNAME на Vercel)
- Проверьте SSL режим в Cloudflare (Full, не Flexible)
- Передеплойте проект

## Оптимизация производительности

Vercel автоматически:
- ✅ Сжимает файлы (gzip/brotli)
- ✅ Кеширует статику на CDN
- ✅ Использует HTTP/2
- ✅ Оптимизирует изображения

Дополнительно в `vercel.json` настроено:
- Кеширование статики на 1 год
- Не кешируется только `index.html`

## Troubleshooting

### Проблема: Домен не подключается

1. Проверьте DNS записи (может занять до 48 часов)
2. Убедитесь, что домен не используется в другом проекте Vercel
3. Проверьте права на домен

### Проблема: Статика не загружается

1. Проверьте `vercel.json` в корне проекта
2. Убедитесь, что `dist/` содержит файлы после сборки
3. Проверьте пути в `dist/index.html` (должны быть `/assets/...`)

### Проблема: React Router не работает

- ✅ Уже решено через `rewrites` в `vercel.json`
- Все маршруты перенаправляются на `/index.html`

## Команды для работы

```bash
# Деплой в preview
vercel

# Деплой в production
vercel --prod

# Просмотр логов
vercel logs

# Список деплоев
vercel ls

# Удалить деплой
vercel remove
```

